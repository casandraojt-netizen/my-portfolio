baseURL = 'https://kentcasandra-portfolio.vercel.app/'
languageCode = 'en-us'
title = 'Kent Casandra'
theme = "PaperMod"

[params]
    env = "production"
    # This is the "Profile Mode" that adds the bio you see on the demo
    [params.homeInfoParams]
        Title = "Hi there! I'm Kent. 👋"
        Content = "SoftDev OJT at Callbox | Automation Specialist"

    # This makes the "My New Hugo Site" at the top look better
    [params.label]
        text = "Kent Casandra"